import styled from 'styled-components';
import { pxToRem } from "@/utils";

export const Container = styled.header`
    // padding: ${pxToRem(0)};
    .container {
        padding-top: 24px;
        padding-bottom: 24px;
    }
`;
